from openrouter import OpenRouter

def call_openrouter(    
    model: str,
    text: str,
    input_type: str,
    dimensions: int,
    api_key: str
) -> dict:
    raise NotImplementedError("Da implementare")